# Feature - Framework - Network

This module provides classes related to networking.

## Highlighted

- **BasicAuthInterceptor**: Interceptor for `OkHttp` that adds Basic Authentication
  headers to requests.
- **executeNetworkRequest**: Helper function to execute a network request and wrap it in
  `kotlin.Result<T>` object. 
